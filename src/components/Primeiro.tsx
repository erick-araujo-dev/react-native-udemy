import { View, Text, SafeAreaView } from "react-native"
import globalStyle from "../styles/globalStyle";

export default () => {
    return(
       <Text style={globalStyle.textGrande}>Testando 123</Text>
    );
}