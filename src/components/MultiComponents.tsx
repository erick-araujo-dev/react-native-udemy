import { View, Text, SafeAreaView } from "react-native"
import globalStyle from "../styles/globalStyle";

const Componente0 = () => {
    return(
       <Text style={globalStyle.textGrande}>Componente 0</Text>
    );
}

export const Componente1 = () => {
    return(
       <Text style={globalStyle.textGrande}>Componente 1</Text>
    );
}

export const Componente2 = () => {
    return(
       <Text style={globalStyle.textGrande}>Componente 2</Text>
    );
}

export default Componente0;